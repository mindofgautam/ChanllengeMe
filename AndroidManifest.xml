<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.Challenge"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="7" />

    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".Challenge"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name="Mainmenu"></activity>
        <activity android:label="@string/app_name" android:name="Single"></activity>
        <activity android:label="@string/app_name" android:name="Multi"></activity>
        <activity android:label="@string/app_name" android:name="Howto"></activity>
        <activity android:label="@string/app_name" android:name="Singlegame"></activity>
        <activity android:label="@string/app_name" android:name="Gameover"></activity>

    </application>
</manifest>